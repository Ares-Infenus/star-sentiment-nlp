# Kaggle API credentials (only needed if downloading from Kaggle)
KAGGLE_USERNAME=your_username
KAGGLE_KEY=your_api_key

# HuggingFace token (only needed for gated models)
HF_TOKEN=your_hf_token

# Device override: "cpu" or "cuda" (auto-detected if not set)
DEVICE=cpu
